export default function HomePage() {
  return (
    <main>
      <h1>CETECH POS engineering scaffold</h1>
      <p>
        This is the CP-05 application foundation. It is not the approved
        production POS UI and does not provide authentication, catalog, pricing,
        payments, or a live sale flow.
      </p>
      <p>
        WS1 owns <code>src/features</code> and <code>src/ui</code>. WS3 owns
        route composition in <code>src/app</code>. Canonical contracts remain
        under <code>docs/contracts</code>.
      </p>
    </main>
  );
}
